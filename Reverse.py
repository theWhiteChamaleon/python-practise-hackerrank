import time
import random

# inputs = input("Enter numbers").split()
# print(inputs)
# start = time.time()
# i = len(inputs)-1
# reverseList = []
# while i >= 0:
#     reverseList.append(inputs[i])
#     i -= 1
# print(float(time.time()-start))

# start = time.time()
# print(reversed(inputs))
# print("reversed time ", float(time.time()-start))

print(random.randint(0, 544))
